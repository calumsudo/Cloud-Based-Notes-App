rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read: if true;  // Allow read access to users collection
      allow write: if true;  // Allow write access if user is authenticated
    }
  }
}
